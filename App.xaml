<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="Kakikomi.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kakikomi">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush x:Key="OpBarBg" Color="#FF0F172A" />
            <SolidColorBrush x:Key="OpPageBg" Color="#FF020617" />
            <SolidColorBrush x:Key="OpPanelBorderBrush" Color="#FF334155" />
            <SolidColorBrush x:Key="OpTextBrush" Color="#FFE2E8F0" />
            <SolidColorBrush x:Key="OpMutedTextBrush" Color="#FF94A3B8" />
            <SolidColorBrush x:Key="PenSwatchRingOuterBrush" Color="#FFF8FAFC" />
            <SolidColorBrush x:Key="PenSwatchRingInnerBrush" Color="#FF38BDF8" />

            <Style x:Key="FlatButtonChromeStyle" TargetType="Button">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="Foreground" Value="#FF0F172A" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <!-- 外側を少し暗いリムで囲み、斜めグラデなしで立体感 -->
                            <Border
                                x:Name="OuterRim"
                                Background="{TemplateBinding BorderBrush}"
                                Padding="2"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <Border
                                    x:Name="RootBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="0"
                                    CornerRadius="{TemplateBinding CornerRadius}">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />
                                            <VisualState x:Name="PointerOver">
                                                <VisualState.Setters>
                                                    <Setter Target="RootBorder.Background" Value="{ThemeResource ButtonBackgroundPointerOver}" />
                                                    <Setter Target="OuterRim.Background" Value="{ThemeResource ButtonBorderBrushPointerOver}" />
                                                    <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ButtonForegroundPointerOver}" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <VisualState.Setters>
                                                    <Setter Target="RootBorder.Background" Value="{ThemeResource ButtonBackgroundPressed}" />
                                                    <Setter Target="OuterRim.Background" Value="{ThemeResource ButtonBorderBrushPressed}" />
                                                    <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ButtonForegroundPressed}" />
                                                    <Setter Target="OuterRim.Padding" Value="2,3,2,1" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Disabled">
                                                <VisualState.Setters>
                                                    <Setter Target="RootBorder.Background" Value="{ThemeResource ButtonBackgroundDisabled}" />
                                                    <Setter Target="OuterRim.Background" Value="{ThemeResource ButtonBorderBrushDisabled}" />
                                                    <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ButtonForegroundDisabled}" />
                                                    <Setter Target="OuterRim.Opacity" Value="0.55" />
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                    <ContentPresenter
                                        x:Name="ContentPresenter"
                                        Padding="{TemplateBinding Padding}"
                                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        Foreground="{TemplateBinding Foreground}" />
                                </Border>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="FlatToggleButtonChromeStyle" TargetType="ToggleButton">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="Foreground" Value="#FF0F172A" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border
                                x:Name="OuterRim"
                                Background="{TemplateBinding BorderBrush}"
                                Padding="2"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <Border
                                    x:Name="RootBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="0"
                                    CornerRadius="{TemplateBinding CornerRadius}">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />
                                            <VisualState x:Name="PointerOver">
                                                <VisualState.Setters>
                                                    <Setter Target="RootBorder.Background" Value="{ThemeResource ToggleButtonBackgroundPointerOver}" />
                                                    <Setter Target="OuterRim.Background" Value="{ThemeResource ToggleButtonBorderBrushPointerOver}" />
                                                    <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ToggleButtonForegroundPointerOver}" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Pressed">
                                                <VisualState.Setters>
                                                    <Setter Target="RootBorder.Background" Value="{ThemeResource ToggleButtonBackgroundPressed}" />
                                                    <Setter Target="OuterRim.Background" Value="{ThemeResource ToggleButtonBorderBrushPressed}" />
                                                    <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ToggleButtonForegroundPressed}" />
                                                    <Setter Target="OuterRim.Padding" Value="2,3,2,1" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Disabled">
                                                <VisualState.Setters>
                                                    <Setter Target="OuterRim.Opacity" Value="0.55" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="Checked">
                                                <VisualState.Setters>
                                                    <Setter Target="RootBorder.Background" Value="{ThemeResource ToggleButtonBackgroundChecked}" />
                                                    <Setter Target="OuterRim.Background" Value="{ThemeResource ToggleButtonBorderBrushChecked}" />
                                                    <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ToggleButtonForegroundChecked}" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="CheckedPointerOver">
                                                <VisualState.Setters>
                                                    <Setter Target="RootBorder.Background" Value="{ThemeResource ToggleButtonBackgroundCheckedPointerOver}" />
                                                    <Setter Target="OuterRim.Background" Value="{ThemeResource ToggleButtonBorderBrushCheckedPointerOver}" />
                                                    <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ToggleButtonForegroundCheckedPointerOver}" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="CheckedPressed">
                                                <VisualState.Setters>
                                                    <Setter Target="RootBorder.Background" Value="{ThemeResource ToggleButtonBackgroundCheckedPressed}" />
                                                    <Setter Target="OuterRim.Background" Value="{ThemeResource ToggleButtonBorderBrushCheckedPressed}" />
                                                    <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource ToggleButtonForegroundCheckedPressed}" />
                                                    <Setter Target="OuterRim.Padding" Value="2,3,2,1" />
                                                </VisualState.Setters>
                                            </VisualState>
                                            <VisualState x:Name="CheckedDisabled" />
                                            <VisualState x:Name="Indeterminate" />
                                            <VisualState x:Name="IndeterminatePointerOver" />
                                            <VisualState x:Name="IndeterminatePressed" />
                                            <VisualState x:Name="IndeterminateDisabled" />
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                    <ContentPresenter
                                        x:Name="ContentPresenter"
                                        Padding="{TemplateBinding Padding}"
                                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        Foreground="{TemplateBinding Foreground}" />
                                </Border>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style
                x:Key="OpButtonStyle"
                TargetType="Button"
                BasedOn="{StaticResource FlatButtonChromeStyle}">
                <Setter Property="Background" Value="#FFF8FAFC" />
                <Setter Property="Foreground" Value="#FF0F172A" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="Padding" Value="18,12" />
                <Setter Property="MinHeight" Value="48" />
                <Setter Property="MinWidth" Value="88" />
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="BorderBrush" Value="#FF475569" />
            </Style>

            <Style
                x:Key="OpToggleStyle"
                TargetType="ToggleButton"
                BasedOn="{StaticResource FlatToggleButtonChromeStyle}">
                <Setter Property="Background" Value="#FFF8FAFC" />
                <Setter Property="Foreground" Value="#FF0F172A" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="Padding" Value="18,12" />
                <Setter Property="MinHeight" Value="48" />
                <Setter Property="MinWidth" Value="88" />
                <Setter Property="CornerRadius" Value="0" />
                <Setter Property="BorderBrush" Value="#FF475569" />
            </Style>

            <Style x:Key="NetaListItemContainerStyle" TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="2" />
                <Setter Property="Margin" Value="0,0,0,6" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListViewItem">
                            <Grid
                                x:Name="Root"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver" />
                                        <VisualState x:Name="Disabled" />
                                        <VisualState x:Name="Selected">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#FF0D9488" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedPointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#FF14B8A6" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedSwiping">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#FF0D9488" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SelectedUnfocused">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#FF64748B" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ペン色: 外枠なし・丸だけ。選択時は白＋黒の二重リング -->
            <Style x:Key="OpPenSwatchToggleStyle" TargetType="ToggleButton">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="0" />
                <Setter Property="Width" Value="44" />
                <Setter Property="Height" Value="44" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid Width="44" Height="44" Background="Transparent">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal">
                                            <VisualState.Setters>
                                                <Setter Target="SelectionRingOuter.Opacity" Value="0" />
                                                <Setter Target="SelectionRingInner.Opacity" Value="0" />
                                                <Setter Target="SwatchScale.ScaleX" Value="1" />
                                                <Setter Target="SwatchScale.ScaleY" Value="1" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="SwatchScale.ScaleX" Value="1.06" />
                                                <Setter Target="SwatchScale.ScaleY" Value="1.06" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="SwatchScale.ScaleX" Value="0.94" />
                                                <Setter Target="SwatchScale.ScaleY" Value="0.94" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="RootGrid.Opacity" Value="0.45" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Checked">
                                            <VisualState.Setters>
                                                <Setter Target="SelectionRingOuter.Opacity" Value="1" />
                                                <Setter Target="SelectionRingInner.Opacity" Value="1" />
                                                <Setter Target="SwatchScale.ScaleX" Value="1.12" />
                                                <Setter Target="SwatchScale.ScaleY" Value="1.12" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="CheckedPointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="SelectionRingOuter.Opacity" Value="1" />
                                                <Setter Target="SelectionRingInner.Opacity" Value="1" />
                                                <Setter Target="SwatchScale.ScaleX" Value="1.16" />
                                                <Setter Target="SwatchScale.ScaleY" Value="1.16" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="CheckedPressed">
                                            <VisualState.Setters>
                                                <Setter Target="SelectionRingOuter.Opacity" Value="1" />
                                                <Setter Target="SelectionRingInner.Opacity" Value="1" />
                                                <Setter Target="SwatchScale.ScaleX" Value="1.08" />
                                                <Setter Target="SwatchScale.ScaleY" Value="1.08" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="CheckedDisabled" />
                                        <VisualState x:Name="Indeterminate" />
                                        <VisualState x:Name="IndeterminatePointerOver" />
                                        <VisualState x:Name="IndeterminatePressed" />
                                        <VisualState x:Name="IndeterminateDisabled" />
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Grid
                                    x:Name="RootGrid"
                                    RenderTransformOrigin="0.5,0.5">
                                    <Grid.RenderTransform>
                                        <ScaleTransform x:Name="SwatchScale" ScaleX="1" ScaleY="1" />
                                    </Grid.RenderTransform>
                                    <Ellipse
                                        x:Name="SelectionRingOuter"
                                        Width="40"
                                        Height="40"
                                        Stroke="{StaticResource PenSwatchRingOuterBrush}"
                                        StrokeThickness="4"
                                        Opacity="0"
                                        IsHitTestVisible="False" />
                                    <Ellipse
                                        x:Name="SelectionRingInner"
                                        Width="34"
                                        Height="34"
                                        Stroke="{StaticResource PenSwatchRingInnerBrush}"
                                        StrokeThickness="2"
                                        Opacity="0"
                                        IsHitTestVisible="False" />
                                    <ContentPresenter
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}" />
                                </Grid>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
